// Preview only: adapter shown transparent with its bolt slots and nut channels
// drawn solid, so the fixing stack reads at a glance.
$fa = 2; $fs = 0.4;
use <../ums_front_blank.scad>
%front_part();
